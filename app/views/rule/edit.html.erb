<%= form_for @rules do |form| %>

  <% if @rules.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rules.errors.count, "error") %>prohibited this product from being saved:</h2>
        <ul>
          <% @rules.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
    </div>
  <% end %>


  <%= render 'form' %>

<% end %>