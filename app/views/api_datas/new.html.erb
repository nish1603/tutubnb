
<div class = "signup">

  <%= form_for @api_data, :url => api_datas_path do |form| %>

  <% if @api_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_data.errors.count, "error") %> prohibited this product from being saved:</h2>
        <ul>
          <% @api_data.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
    </div>
  <% end %>


    <fieldset>
    	<legend>Enter your details</legend>

    	<div>
    		<%= form.text_field :url, placeholder: "Enter callback for your site:" %>
    	</div>

      <div>
        <%= form.submit "Register Api" %>
      </div>

    </fieldset>
  <% end %>
</div>