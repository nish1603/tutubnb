
$(document).ready(function(){
  $("#deal_start_date").datepicker({ numberOfMonths : 2, minDate : new Date(), dateFormat : "dd/mm/yy", onSelect : function(date_text, inst) {
  		$("#deal_end_date").datepicker({ numberOfMonths : 2, minDate : new Date($("#deal_start_date").val()), dateFormat : "dd/mm/yy" });
  	} });
  $("#deal_end_date").datepicker({ numberOfMonths : 2, minDate : new Date(), dateFormat : "dd/mm/yy" });

  $("#deal_start_date").datepicker({
  	onSelect : function(date_text, inst) {
  		$("#deal_end_date").datepicker({ numberOfMonths : 2, minDate : new Date($("#deal_start_date").val()), dateFormat : "dd/mm/yy" });
  	} 
  });

});
